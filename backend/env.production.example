# ========================================
# Backend Environment - Production (LAN)
# ========================================
# ⚠️ Backend uses host network - PORT must match external port

# Server Configuration
PORT=3109
NODE_ENV=production
API_PREFIX=/api

# Database Configuration (MySQL)
DB_HOST=192.168.0.96
DB_PORT=3306
DB_USER=jitdhana
DB_PASSWORD=iT12345$
DB_NAME=manufacturing_system
DB_TIMEZONE=+07:00
DB_CONNECTION_LIMIT=20
DB_DATE_STRINGS=true

# CORS Configuration
CORS_ORIGIN=*

# Logging
LOG_LEVEL=info

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100

# Security
HELMET_ENABLED=true

