# ========================================
# Docker Compose - Production Environment
# ========================================
# Reference: DEV_STANDARD.md Section 17 (Deployment Summary)
# Modified: Direct access without nginx (LAN deployment)

version: '3.8'

services:
  # Frontend Service
  frontend:
    build:
      context: ..
      dockerfile: Dockerfile.frontend
    container_name: production-schedule-frontend
    ports:
      - "3019:3017"  # External:Internal - เปลี่ยนเป็น 3019
    env_file:
      - ../.env.production
    environment:
      - NODE_ENV=production
    restart: always
    networks:
      - prod-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3017"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Backend API Service
  backend:
    build:
      context: ../backend
      dockerfile: Dockerfile.js
    container_name: production-schedule-backend
    network_mode: "host"  # ใช้ host network เพื่อเชื่อมต่อ DB ด้วย IP ของ host
    env_file:
      - ../backend/.env.production
    environment:
      - NODE_ENV=production
    restart: always
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3109/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  prod-network:
    driver: bridge

# ========================================
# Nginx Reverse Proxy (Disabled for LAN deployment)
# ========================================
# Uncomment if you need reverse proxy in the future:
#
# services:
#   nginx:
#     image: nginx:alpine
#     container_name: production-schedule-nginx
#     ports:
#       - "8080:80"
#     volumes:
#       - ./nginx.conf:/etc/nginx/nginx.conf:ro
#     restart: always
#     networks:
#       - prod-network
#     depends_on:
#       - frontend
#       - backend

# ========================================
# Deployment Commands (Production):
# ========================================
# Build & Start:
#   docker compose -f infra/docker-compose.prod.yml up -d --build
#
# View Logs:
#   docker compose -f infra/docker-compose.prod.yml logs -f
#
# Stop:
#   docker compose -f infra/docker-compose.prod.yml down
#
# Restart Specific Service:
#   docker compose -f infra/docker-compose.prod.yml restart backend
#
# Health Check:
#   docker compose -f infra/docker-compose.prod.yml ps
#
# ========================================
# Access URLs (LAN):
# ========================================
# Frontend: http://SERVER_IP:3019
# Backend API: http://SERVER_IP:3109/api/
# Health Check: http://SERVER_IP:3109/api/health
#
# Note: Backend uses host network to access MySQL with host IP
# Backend internal port must be set to 3109 in backend/.env.production
